{% extends "base.html.twig" %}
{% block header %}
    {% block navbar %}


        <div class="header_container">
            <div class="container">
                <div class="row">
                    <div class="col">
                        <div class="header_content d-flex flex-row align-items-center justify-content-start">
                            <div class="logo_container">
                                <!--  <a href="#">
                                      <div class="logo_text">Unic<span>at</span></div>
                                  </a>-->
                                <img src="{{ asset('images/thinklance2.png') }}" height="60" width="150">
                            </div>

                            <nav class="main_nav_contaner ml-auto">
                                <ul class="main_nav">
                                    <li>
                                        <a href="{{ path('fos_user_profile_show') }}">{{ app.user.username | capitalize }}</a>
                                    </li>
                                    <li class="active"><a href="{{ path('auth_homepage') }}">Accueil</a></li>
                                    <li><a href="{{ path('projets') }}">Mes Projets</a></li>
                                    <li><a href="{{ path('afficheDemande') }}">Mes demandes</a></li>
                                    <li class="dropdown dropdown-extended dropdown-notification" id="header_notification_bar">
                                        <a href="javascript:;" class="dropdown-toggle" data-toggle="dropdown" data-hover="dropdown" data-close-others="true">
                                            <i class="icon-bell"> <img src="{{ asset('images/notification.png') }}" height="20" width="30"></i>
                                            <span class="badge badge-default"></span>
                                        </a>
                                    </li>
                                    {#<li><a href="blog.html">Blog</a></li>#}
                                    {#<li><a href="#">Page</a></li>#}
                                    {#<li><a href="contact.html">Contact</a></li>#}
                                    <li><a href="{{ path('fos_user_security_logout') }}">
                                            {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                                        </a>
                                    </li>
                                </ul>
                                {#<div class="search_button"><i class="fa fa-search" aria-hidden="true"></i></div>#}
                                <!-- Hamburger -->
                                <div class="hamburger menu_mm">
                                    <i class="fa fa-bars menu_mm" aria-hidden="true"></i>
                                </div>
                            </nav>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    {% endblock %}
{% endblock %}
{% block content0 %}
    <br>
    <br>
    <br>
    <br>
    <br>
    <br><br>
    <br>
    <br>
{% endblock %}

{% block content %}

    {#<h1>{{ tache.projet }}</h1>#}
    <a href="{{ path('ajoutTache') }}">+Tâches</a>
<center>
<h3>Liste des Tâches</h3><br><br>
<table border="2">
    <tr><td>Etat d'une tâche</td>
        <td>Description d'une tâche</td>
        <td></td>
    </tr>
    {% for taches in tache %}
        <tr>
            <td><center>
                   {% if( taches.etatTache=="done" ) %}
                     <input type="checkbox" id="etat" name="etat" checked>
                   {% else  %}
                       <input type="checkbox" id="etat" name="etat" >
                    {% endif %}
                </center> </td>
            <td>  {{ taches.nomTache }} </td>
            <td> <a href="{{ path('modifierTâche',{'id': taches.id}) }}" class="btn btn-light">modifier la discription </a>
                <a href="{{ path('supprimerTâche',{'id': taches.id}) }}" class="btn btn-danger delete-activite">Supprimer </a>

            </td>
        </tr>
    {% endfor %}

</table>
    <br> <br>
    <button name="update" type="submit"> Projet Fini!</button>
<script src="{{ asset('js/jquery.js') }}"></script>
<script>
    // $('#etat').mouseleave(
    //     function () {
    //        tache.etatTache = ('#etat').valueOf();
    //         // todo else
    //     });


    $("#etat").live("click", function() {
        if($(this).is(":checked"))
            tache.etatTache.val("done");
    else
            tache.etatTache.val("to do");
    });

    </script>

</center>
{% endblock %}
