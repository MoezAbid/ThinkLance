<div>

    {% for notif in notifications %}
        {% if notif.idFreelancer == app.user  %}
    <li>
        <div class="job-listing">
            <div class="job-title-sec">

                <div class="c-logo round"> <img src="{{ asset('images1/') }}{{app.user.nomImage }}" height="50px" width="50px" alt="" /> </div>
                <h3><a href="#" title="">{{ notif.idEmployer }}</a></h3>
                <span>a commenté votre profil</span>
            </div>
        </div>
    </li>

        {% endif %}
    {% endfor %}
{#    <h5>Notifications</h5>#}
{#
    <ul id="notifications">
        {% for notif in notifications %}
               <li>{{ notif.title }}</li>
               <li>{{ notif.description }}</li>
               <li><a href="{{ path(notif.route,notif.parameters) }}" >See</a></li>
               {% else %}
               <li>List is empty</li>
           {% endfor %}


    </ul>


</div>#}
